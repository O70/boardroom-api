spring:
  application:
    name: thraex-boardroom
  jackson:
    time-zone: GMT+8
    default-property-inclusion: NON_NULL
  datasource:
    url: jdbc:mysql://localhost:3306/mydb?serverTimezone=GMT
    username: root
    password: hanzo

server:
  port: 8037

logging:
  level:
    org.springframework.web.socket: debug
